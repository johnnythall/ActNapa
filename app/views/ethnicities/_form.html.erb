<%= form_for(@ethnicity) do |f| %>
  <% if @ethnicity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ethnicity.errors.count, "error") %> prohibited this ethnicity from being saved:</h2>

      <ul>
      <% @ethnicity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
